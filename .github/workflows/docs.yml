name: Documentation

on:
  push:
    branches: [main]
    paths:
      - "docs/**"

jobs:
  placeholder:
    name: Documentation Build (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Documentation site build will be configured in Phase 11 (Docusaurus)."
